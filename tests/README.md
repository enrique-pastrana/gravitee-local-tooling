# Tests

Run lightweight local tests with:

```bash
python3 -m unittest discover -s tests
```

These tests avoid Docker and external services. They cover manifest generation and agent config patching.
